name: "Build"
on:
  - workflow_dispatch
jobs:
  test:
    uses: ./.github/workflows/main.yml
    with:
      mode: '["build"]'
      alphas: '["qwerty"]'
      nav: '["default"]'
      clipboard: '["mac"]'
      layers: '["default"]'
      kmonad_os: '["mac"]'
