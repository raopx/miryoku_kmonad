name: "Build"
on:
  - workflow_dispatch
jobs:
  test:
    uses: ./.github/workflows/main.yml
    with:
      mode: '["build"]'
      alphas: '["default"]'
      nav: '["default"]'
      clipboard: '["mac"]'
      layers: '["default"]'
      mapping: '["default"]'
      kmonad_os: '["mac"]'
